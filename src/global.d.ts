export interface LiquidityToken {
  // coingeckoID: string;
  decimals: number;
  // logo: string;
  mint: string;
  // name: string;
  symbol: string;
  // volume24h: string;
}

export interface TokenCount {
  symbol: String,
  target: number,
}
